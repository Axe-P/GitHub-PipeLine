# .github/workflows/cypress-tests.yml
name: Cypress Tests

on:
  pull_request:
    branches:
      - develop

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run Cypress tests
        run: |
          npm run test # Run all tests
          npm run test-component # Run component tests
          npm run test-gui # Optionally run GUI tests (this opens the GUI, so might not be suitable for CI)
        env:
          CI: true